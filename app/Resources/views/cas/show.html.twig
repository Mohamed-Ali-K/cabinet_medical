{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ca</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ca.id }}</td>
            </tr>
            <tr>
                <th>Nomcas</th>
                <td>{{ ca.nomCas }}</td>
            </tr>
            <tr>
                <th>Examentype</th>
                <td>{{ ca.examenType }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('Cas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('Cas_edit', { 'id': ca.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
